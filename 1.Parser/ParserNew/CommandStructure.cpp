#include "CommandStructure.hpp"

template <typename Operand>
CommandStructureUnderlyingType<Operand> CommandStructure<Operand>::addArgument(std::string line) {
    /*
    while (line not empty):
        take 2 tokens
        add them to command
    */
    
    while (!line.empty()) {
        
    }
}