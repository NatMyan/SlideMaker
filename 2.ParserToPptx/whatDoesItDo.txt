run();

readInput();
parseCommand(); isCommandValid();
computeResult();
printResult();

Controller: basically
    while program is running:
        get input
        take first token 

        if that command is found in command reg:
            take the following 2 tokens
            parse them
            addArgument to command

