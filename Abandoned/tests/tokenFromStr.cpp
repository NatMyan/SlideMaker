/*
#include <iostream>
#include <sstream>
#include <string>

int main() {
    std::string inputString = "Hello World 42 \n"; // Example string with tokens
    std::istringstream iss(inputString);
    
    std::string token;
    
    while (iss >> token) {
        // The first token is now stored in the 'token' variable
        std::cout << "Token: " << token << std::endl;
    } 
    
    return 0;
}
*/